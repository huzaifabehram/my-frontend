# Local development — copy to .env and adjust if your backend runs elsewhere
REACT_APP_API_URL=http://localhost:5000

# Production (set in Vercel → Settings → Environment Variables)
# REACT_APP_API_URL=https://api.motiviam.com
